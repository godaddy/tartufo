dist: xenial   # required for Python >= 3.7
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7"
  # PyPy versions
  - "pypy3.5"
install: pip install -e .[tests]
script: ./run_lints
        pytest --cov=./ && codecov
deploy:
  provider: pypi
  user: "mxhenry"
  password:
    secure: $PYPI_PASS
  on:
    branch: publish_to_pypi
