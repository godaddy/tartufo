dist: xenial   # required for Python >= 3.7
language: python
python:
  - "3.7"
install: pip install -e .[tests]
script: ./scripts/lint_and_test
deploy:
  provider: pypi
  user: $PYPI_USER
  password:
    secure: $PYPI_PASS
  on:
    branch: master
